<div class="card">
  <div class="card-header">
    <h2>Edit Company Value: <%= @company_value.title %></h2>
  </div>
 
  <%= render 'form', company_value: @company_value %>

<%= link_to "Back", admin_users_company_values_path, class: 'btn btn-outline-secondary' %>