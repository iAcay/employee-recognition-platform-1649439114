<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover table caption-top">
    <caption class="text-center"><h1>Kudos Admin Panel &#128110;</h1></caption>
    <thead class="table-dark">
      <tr class="text-center">
        <th>Title</th>
        <th>Content</th>
        <th>Giver</th>
        <th>Receiver</th>
        <th>Modification</th>
      </tr>
    </thead>

    <tbody>
      <% @admin_users_kudos.each do |admin_users_kudo| %>
        <tr class="text-center">
          <td><%= admin_users_kudo.title %></td>
          <td><%= admin_users_kudo.content %></td>
          <td><%= admin_users_kudo.giver.email %></td>
          <td><%= admin_users_kudo.receiver.email %></td>
          <td><%= link_to 'Destroy', admin_users_kudo_path(admin_users_kudo), method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-outline-danger' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to 'Back to Admin Dashboard', dashboard_admin_users_pages_path, class:'btn btn-outline-secondary' %> 
</div>